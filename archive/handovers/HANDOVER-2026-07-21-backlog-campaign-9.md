# Handover — resolve FMC-15 (launcher.py duplicate-instance ownership + lost replies across reconnect/shutdown) — seventh issue of the 8-issue queue (FMC-9..16)

**Date**: 2026-07-21 | **Grounded against**: `dev` @ `f5b7eea` (1 commit ahead of `origin/dev`, unpushed at write time — push it in restore's preflight), clean | **Tracker**: doc-1

## Paste-ready prompt for the next session

```
Run /backlog-handover restore in /Users/jdnewhouse/repos/fast-mcp-claude. Tracker: doc-1.
Cursor: FMC-15 -- launcher.py has three bugs, all around duplicate-instance ownership and
lost replies. (1) IDENTITY_LIVE_ELSEWHERE (another live process already owns this launcher
identity) is logged once but never enforced -- the heartbeat loop keeps looping, nothing
stops the bridge or the poll loop from claiming work. (2) The stale-claim reaper runs before
this process's OWN ownership is confirmed (before the first successful announce on this
connection), so a duplicate/illegitimate second instance reaps the real owner's genuinely
in-flight tasks out from under it. (3) A task handler's closure-captured client connection
goes stale on reconnect (heartbeat-detected dead session, transport error, server restart);
the handler keeps running against the OLD connection and its reply silently fails and is
lost -- meanwhile the reaper explicitly skips reaping it because it still looks in-flight, so
it's simultaneously unreapable and undeliverable. (4) SIGTERM/SIGINT cancellation unwinds
through the client's own async-with __aexit__ BEFORE reaching the shutdown handler, so the
module's own documented ALWAYS-REPLY-on-every-exit-path invariant's shutdown sweep runs
against an already-closed connection and silently fails to reply to any genuinely in-flight
task. Queue order (isolation/complexity-first: FMC-10 [done], FMC-14 [done], FMC-11 [done],
FMC-12 [done], FMC-9 [done], FMC-13 [done], FMC-15, FMC-16) confirmed by the user on
2026-07-21 -- do not re-ask before taking the next item. This is flagged in the tracker as
"Large, async-heavy" -- budget more time/context for it than recent sessions.
```

## State
| Item | Status |
| --- | --- |
| Branch | `dev` @ `f5b7eea`, clean |
| Sync with origin | 1 ahead / 0 behind `origin/dev` (this session's archive-handover commit not yet pushed — push it as part of restore's preflight, or it happens automatically if you push before starting FMC-15's branch) |
| Leftover `feature/*` branches (this campaign) | none real. `git branch -a` still lists `remotes/origin/feature/FMC-13` and `remotes/origin/feature/FMC-9` — both confirmed via `git ls-remote --heads origin feature/FMC-13` / `feature/FMC-9` (both empty output) to be genuinely deleted remotely; these are stale local remote-tracking-ref cache entries (clears on `git fetch --prune`), not actual litter. This is the same pattern noted in the last two handovers — expect it to keep recurring until someone runs `git fetch --prune` locally; it is cosmetic and never blocks anything. |
| Unrelated branches present | `chore/start-session-symlinkable`, `feat/eca-100-lane-reconfig`, `feat/eca-72-supervisor-pre-adoption`, `feat/session-to-session-messaging`, `feat/teams-relay`, `fix/teams-outbox-operator-direct` — all pre-existing, unrelated to this campaign's FMC- numbering; not campaign litter, do not touch. |
| Open PRs | none (`gh pr list --state open` empty) |
| Tracker cursor | FMC-15 (doc-1, updated and committed this session) |
| FMC-15 task status | To Do, unassigned, Priority High, Type bug |
| Queue after FMC-15 | FMC-16 (last item in the queue) |

## Next steps
1. Preflight per the skill: confirm `git status --porcelain` is clean and push the 1-ahead commit to `origin/dev` if not already done.
2. `git checkout -b feature/FMC-15 dev`.
3. Read `backlog instructions task-execution`; `backlog task view FMC-15 --plain` for the full self-contained description (already reviewed this session — see Critical context below for a summary); mark In Progress + assign; record implementation plan.
4. **This task is materially larger than recent sessions in the queue.** It touches `src/fast_mcp_claude/launcher.py`'s core bridge loop (`_bridge`), its heartbeat task, its stale-claim reaper (`_reap_stale_claims`), its per-task handler (`_handle_task`), and its shutdown routine (`_shutdown`) — four interacting pieces of a single async state machine, not four independent one-line fixes. Read the WHOLE file before touching anything; the task's own line-number references were written fresh this session (2026-07-21) against the current code, so they should still be accurate, but verify against the actual file rather than assuming (the FMC-13 handover found channel.py's cited lines had drifted from concurrent out-of-campaign work — launcher.py has not been touched by anything else recently as far as this session observed, but check `git log -5 -- src/fast_mcp_claude/launcher.py` before trusting that).
5. **AC#1+#2 are closely related (both about the IDENTITY_LIVE_ELSEWHERE / ownership-confirmed gate) and probably want a shared runtime flag/event**, similar in shape to `channel.py`'s `_Runtime.announce_confirmed: asyncio.Event` (ECA-71 Layer B — see `src/fast_mcp_claude/channel.py`'s `_presence_loop`/`_inbox_loop`, just fixed again this session in FMC-13). launcher.py may already have something structurally similar (check for an existing `_Runtime`-like class or module-level state) or may need one added. Read how `channel.py` solved the *exact same class of problem* (a confirmed-ownership gate + a refused-announce disarm) before designing launcher.py's fix from scratch — this campaign has already built and battle-tested that pattern once in this same codebase, including its own regression tests (`tests/test_channel.py`'s `test_inbox_does_not_claim_until_announce_confirmed` et al.) as a design + test template.
6. **AC#3 (reconnect-safe reply) and AC#4 (shutdown-safe reply) are both fundamentally "a task handler needs to reply using a connection that is guaranteed live/valid," which is a different problem shape from AC#1/#2.** Consider whether they share a fix (e.g., a dedicated, always-fresh client used specifically for replies, decoupled from the main bridge connection's lifecycle; or a retry-on-reconnect queue for pending replies) — re-read the task's own exact AC wording before deciding scope, and don't over-invent a generalized reconnect framework if a narrower fix satisfies both ACs. This campaign's established norm (see FMC-9/FMC-13's handovers) is: narrow, behavioral, AC-scoped fixes over redesigns, and asking the user before expanding scope if the minimal fix turns out to be genuinely ambiguous.
7. Add regression tests (AC#5): one per named case (rogue-second-instance-under-IDENTITY_LIVE_ELSEWHERE-does-not-reap-or-claim, reply-survives-mid-task-reconnect, reply-survives-shutdown-cancellation), each confirmed to fail pre-fix / pass post-fix via `git stash`. Check whether `tests/test_launcher.py` already exists and what harness patterns it uses (mock client, real HTTP server, etc.) before designing new tests — follow its existing conventions rather than inventing a new pattern, same as every prior session in this campaign has done for its target file's existing test file.
8. Read `backlog instructions task-finalization` before checking any AC. Verify AC#1-5 with objective evidence.
9. **Given this task's size and the campaign's established pattern for concurrency/security-boundary fixes, budget for an adversarial subagent review of the full branch diff before opening the PR** — the last four such fixes in this campaign (FMC-11, FMC-12, FMC-9, and FMC-13's own bounced-message-tracking gap) each had a real bug caught by adversarial review that self-review missed. This file has MORE interacting moving parts than channel.py's arm/claim/reply state machine did, so if anything this review matters more here, not less.
10. Update the tracker (doc-1) on the branch: move FMC-15 to Resolved, advance cursor to FMC-16 (the last item), append the session-15 log entry.
11. Commit, review, push, open+merge PR into `dev` via `gh pr merge --rebase --delete-branch`. Check `origin/dev` for concurrent unrelated commits before assuming a clean merge (this has bitten FMC-9's session before, though FMC-13's session found a clean base — don't assume either way, always check). Sync local `dev`, delete local branch, verify remote deletion with `git ls-remote --heads origin feature/FMC-15`.
12. Archive this handover to `archive/handovers/` (check for name collisions — `-2.md` through `-8.md` already exist for 2026-07-21, so this becomes `-9.md`), commit, write the session-16 handover for cursor FMC-16 (the LAST item in the queue — flag in that handover that the queue will be empty after FMC-16, so session 16 should end with the campaign-complete summary + suggest `init` for a fresh queue, per the skill's own "Queue empty instead?" branch), push `dev`.

## Critical context / traps
- **This campaign's tracker is doc-1, not doc-2.** doc-2 is the Codex full-codebase review report FMC-9..16 were generated from — read it only if FMC-15's own self-contained description isn't enough.
- **FMC-15 is explicitly flagged in the tracker's own queue notes as "Large, async-heavy."** It was deliberately sequenced second-to-last (isolation/complexity-first ordering) for exactly this reason. Don't assume it fits the same ~1-2 hour shape recent sessions (FMC-9/FMC-11/FMC-12/FMC-13) did — budget more turns/context, and don't hesitate to ask the user if genuine design ambiguity comes up (same standing permission FMC-9/FMC-13 used for their own harder calls).
- **FMC-16 (the last queue item) is ALSO in launcher.py** — same file, sequenced right after FMC-15 specifically to reduce rebase churn (per the tracker's queue note), mirroring how FMC-13 was sequenced right after FMC-9 for the same reason in channel.py. Expect FMC-16's session to re-read a launcher.py that FMC-15 already substantially changed — the FMC-16 session should not trust any line numbers from FMC-16's own task description without re-verifying against the actual post-FMC-15 file, same lesson FMC-13's session already had to apply for channel.py.
- **channel.py's ECA-71 owner-token pattern (`announce_token`, `IDENTITY_LIVE_ELSEWHERE`, `Runtime.announce_confirmed: asyncio.Event`, the presence-loop-sets/inbox-loop-waits handshake) is the direct prior art for FMC-15's AC#1+#2.** It was built, adversarially reviewed, and battle-tested across several sessions in this exact campaign (see `channel.py`'s `_presence_loop`/`_inbox_loop` and CLAUDE.md's description of "ECA-71 / ADR-0029: owner-token identity guard"). Read it before designing launcher.py's fix — don't reinvent this from scratch when a working, tested pattern already exists one file over in the same codebase.
- **This is explicitly a bugfix task, not a request to redesign launcher.py's connection/reply architecture from scratch.** The task's acceptance criteria are narrow and behavioral (AC#1-5 above) — satisfy those without inventing unnecessary new abstractions unless the narrow fixes genuinely require it (same judgment call every prior FMC- session in this campaign has made and documented).
- **From FMC-11/FMC-12/FMC-9/FMC-13's sessions**: adversarial subagent review of a security-boundary/concurrency fix has now caught a real, previously-unnoticed bug in every one of the last FOUR such fixes in this campaign (a discarded `os.write()` return value in FMC-11; a waiter-refcount-vs-eviction ordering gap in FMC-12; a blind-timer bypass in FMC-9; a scalar-vs-bounded-history gap in FMC-13's own bounced-message tracking). This is a hard requirement at this point for any concurrency/reconnect/shutdown-ordering fix, which FMC-15 clearly is — do not skip it or treat it as optional.
- **From FMC-14's session (session 10)**: a bare `pytest.raises(Exception)` regression test can pass against the very bug it's meant to catch. Always assert the *specific* behavior/state transition a fix's regression test is meant to distinguish.
- **`gh pr merge --delete-branch` works cleanly when the tree is clean AND `origin/dev` hasn't moved under the PR.** When it has moved, the fix is `git fetch && git rebase origin/dev`, resolve conflicts commit-by-commit, re-run the full test suite + ruff after the rebase, `git push --force-with-lease`, then retry `gh pr merge --rebase --delete-branch`. The remote-tracking ref for a deleted branch can linger in `git branch -a` until a `git fetch --prune` — that's a stale local cache, not an actual leftover branch; `git ls-remote --heads origin feature/<KEY>` is the authoritative check (this pattern has now recurred across FMC-9 and FMC-13's sessions — expect it again, it is harmless).

## Do not repeat
- Don't re-run `/backlog-handover init` when a tracker/queue/cursor/handover already exist and are current — this is the established norm for this campaign (confirmed explicitly in session 9).
- Don't assume a library's/framework's API shape from memory or general docs — verify against the actual installed/available version/platform before choosing an approach. FMC-14 (fastmcp Client API), FMC-11 (`os.scandir` dir_fd support), FMC-12 (`_db_lock`'s actual scope), and FMC-9 (`fastmcp.server.dependencies.get_access_token()`'s actual resolution behavior) all hit this discipline; apply it again if launcher.py's fix touches anything in fastmcp's Client reconnect/lifecycle behavior.
- Don't write a regression test with a bare `pytest.raises(Exception)` (or equivalent loose assertion) when the point is distinguishing "behaves correctly for the right reason" from "happens to also pass for an unrelated reason." FMC-14's adversarial review caught exactly this.
- Don't trust a single self-review as sufficient for a concurrency/state-machine fix — the last four adversarial reviews in this campaign (FMC-11, FMC-12, FMC-9, FMC-13) each found a real bug the self-review missed. Budget for that extra pass as a required step, not an optional one.
- Don't assume `origin/dev` is still at the SHA you branched from when opening the PR — always re-check (FMC-9's session hit exactly this; FMC-13's session checked and found a clean base — the check itself is the norm, not a specific expected outcome).
- Don't invent an unverifiable behavioral signal when the task's own text (or reasoning from first principles about what's actually observable) suggests no such signal cleanly exists — FMC-13's AC#1 explicitly avoided inventing an unverifiable MCP client-capabilities check for exactly this reason, scoping to what was concrete and testable instead. If FMC-15's reconnect/shutdown fixes hit a similar "we can't fully verify this from inside the process" wall, prefer the same narrow, defensible, testable scope over a speculative broader fix.
- When a tracked scalar needs to represent "one of possibly several recent events" (not just "the most recent one"), don't default to a single scalar field — FMC-13's adversarial review caught exactly this shape of bug (`bounced_message_id` as a scalar instead of a bounded collection). If FMC-15's fixes track any similar "recent state" (e.g., which messages were affected by a reconnect, which tasks are pending shutdown-reply), consider up front whether more than one can be live at once.
