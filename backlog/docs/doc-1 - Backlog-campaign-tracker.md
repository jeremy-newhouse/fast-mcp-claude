---
id: doc-1
title: Backlog campaign tracker
type: other
created_date: '2026-07-20 20:49'
updated_date: '2026-07-21 15:32'
---
# Backlog campaign tracker

One issue per session. Protocol: restore → take the cursor issue → feature-branch
lifecycle → advance cursor → append session log → write handover.

## Cursor
**Next issue: FMC-10** — queue order confirmed by the user on 2026-07-21 ("Isolation/complexity first (recommended)"); do not re-ask before taking the next item.

## Queue (confirmed order)
| # | Issue | Type | One-line note |
| --- | --- | --- | --- |
| 1 | FMC-10 | bug | server.py/__main__.py: MCP auth fails open when MCP_API_KEY is unset/empty; startup log misreports it as enabled. Small, isolated. |
| 2 | FMC-14 | bug | hook.py: Client(headers=...) incompatible with installed fastmcp 3.4.4, permission relay dead for authenticated deployments. Small, isolated. |
| 3 | FMC-11 | bug | files.py/validation.py: TOCTOU symlink race between validate_workspace_path and actual file-bridge use. Moderate. |
| 4 | FMC-12 | bug | store.py: non-atomic Teams/session-relay drain claims + unbounded Notifier growth via adversarial identities/channels. Moderate; touches FMC-5's territory (see that task's notes) but is a distinct gap, not a duplicate. |
| 5 | FMC-9 | bug | channel.py/messaging.py: forgeable triggering_admin metadata auto-allows tool calls; send_teams operator_direct mislabeling. CRITICAL severity, design-heavy — needs a real trust mechanism, not just a bugfix. |
| 6 | FMC-13 | bug | channel.py: two-part arming gate is one-part in code; claimed-message state race-prone (3 sub-races). Same file as FMC-9 — sequenced right after to reduce rebase churn. |
| 7 | FMC-15 | bug | launcher.py: duplicate-instance ownership unenforced; replies lost across reconnect/shutdown. Large, async-heavy. |
| 8 | FMC-16 | bug | launcher.py: unbounded subprocess output buffering; incomplete process-group kill; unsupervised approval-relay startup race. Same file as FMC-15 — sequenced right after. |

## Resolved
| # | Issue | Status/date/session | Evidence summary |
| --- | --- | --- | --- |
| 1 | FMC-7 | Done, 2026-07-20, session 1 | Doc-only: fixed the permission-relay implemented-vs-not-yet contradiction (CLAUDE.md + README.md, 3 locations); added launcher.py/session.py/session_hook.py to CLAUDE.md module layout + presence.forget + session_relay 'check' op; corrected traversal-defense claim; softened CLAUDE.md's absolute no-central-hub claim; added README Standalone tooling section (worker-supervisor/spawner/sandbox-runner/start-session.sh) + fixed channel source-attribute example + completed the 8-tool teams_outbox/session_relay table rows; created spawner/README.md; rewrote start-session.sh header comment. Verified via grep/read of current source against every claim; `ruff check src/ tests/` and `bash -n start-session.sh` both passed. All 5 ACs checked. |
| 2 | FMC-3 | Done, 2026-07-20, session 2 | Fixed 2 bugs in src/fast_mcp_claude/auth.py::ApiKeyVerifier.verify_token: (1) process-global self-perpetuating lockout — reordered so a correct token is compared and accepted BEFORE consulting the rate limiter, so it always succeeds even during an active lockout (verified verify_token receives no per-connection identity to scope the limiter to just the attacker); (2) non-ASCII bearer crash — hmac.compare_digest now runs on UTF-8-encoded bytes instead of raw str, so a non-ASCII token yields a clean None/401 instead of an uncaught TypeError, and is correctly counted as a rate-limiter failure. Added 3 tests to tests/test_auth.py covering both scenarios. Verified: `uv run pytest tests/test_auth.py -v` (10 passed), full `uv run pytest` (251 passed), `uv run ruff check src/ tests/` (clean). All 3 ACs checked. |
| 3 | FMC-2 | Done, 2026-07-20, session 3 | Fixed the announce_token leak in who() (src/fast_mcp_claude/tools/presence.py): redact credential-shaped metadata keys (reusing logging_config.py's existing SENSITIVE_LOG_FIELDS_EXACT/SENSITIVE_LOG_SUFFIXES blacklist) from each peer's metadata at the who() tool boundary, not in the shared store.list_presence()/_row_to_presence() layer (existing tests rely on that layer still exposing the raw token to verify the ECA-71 guard end-to-end). forget()/announce()'s owner-token guard needed no change — it reads the token via its own raw SQL query, never through the redacted path. Added 2 tests to tests/test_presence.py. Verified: `uv run pytest tests/test_presence.py -v` (15 passed, all pre-existing tests unmodified and passing), full `uv run pytest` (253 passed), `uv run ruff check src/ tests/` (clean). All 3 ACs checked. |
| 4 | FMC-8 | Done, 2026-07-20, session 4 | Verify-first bug, resolved as CONFIRMED BROKEN (AC#1), not the no-op AC#3 branch. Verified via two independent sources (official Claude Code docs fetched by a research subagent, and direct `strings` inspection of the installed claude CLI v2.1.216 binary) that Claude Code prefixes MCP tool names using the .mcp.json config KEY (`mcp__<key>__<tool>`), never the server's self-declared initialize-handshake name — confirming channel.py's own in-code comment. This meant .mcp.json.example's channel entry key ("claude-channel") really did mismatch channel.py's hardcoded SERVER_NAME ("fast-mcp-claude-channel"), breaking the permission relay's always-allow check for the channel's own reply/send_teams/list_sessions/send_to_session/check_session_message tools for anyone following .mcp.json.example + README.md literally (start-session.sh's generated config already used the correct key, so that path was unaffected). Fixed (AC#2) by aligning every documented/example config key with SERVER_NAME rather than making SERVER_NAME dynamic (narrower, lower-risk): .mcp.json.example, README.md, .claude/commands/worker.md, .claude/settings.example.json. Added 3 regression tests in tests/test_channel.py that parse those files at test time and assert the key/flag equals channel_mod.SERVER_NAME. Verified: `uv run pytest tests/test_channel.py -v` (92 passed), full `uv run pytest` (257 passed), `uv run ruff check src/ tests/` (clean). ACs #1 and #2 checked; #3 left unchecked as not applicable (mutually exclusive with the confirmed-broken outcome). |
| 5 | FMC-4 | Done, 2026-07-21, session 5 | Fixed 2 bugs. (1) Sandbox existence oracle: reordered validate_workspace_path (utils/validation.py) so the WORKSPACE_ROOTS containment check runs before must_exist — empirically confirmed the pre-fix code raised different exception types (PermissionDeniedError vs ValidationError) for an existing vs missing out-of-sandbox path by exec'ing the old function standalone against a tmp dir; post-fix both raise PermissionDeniedError uniformly. Left config.py:232's symlink-resolve logic untouched per the task's own note. (2) Body-size caps: added a generic validate_json_object_size() helper plus MAX_METADATA_BYTES=256_000/MAX_TOOL_INPUT_BYTES=1_000_000/MAX_TOOL_NAME_BYTES=256, wired into all 6 flagged fields (send_prompt.metadata, request_approval.tool_name/tool_input, announce.metadata, request_session_op.payload, complete_session_op.result, request_teams_send.metadata); validate_pubsub_payload refactored to delegate to the same helper. Added 14 validator unit tests + 8 tool-layer wiring tests (2 new files: test_messaging.py, test_permissions.py). Verified: `uv run pytest` (277 passed, up from 257), `uv run ruff check src/ tests/` (clean; pre-existing `ruff format` drift on 9 files, unrelated to this branch, confirmed via git stash). All 3 ACs checked. |
| 6 | FMC-5 | Done, 2026-07-21, session 6 | Fixed all 4 Notifier/long-poll correctness bugs in services/store.py. (1) Broadcast messages now wake every identity-scoped waiter via a new Notifier.notify_prefix(), not just the wildcard inbox:* key. (2) Notifier._events no longer grows unbounded: a new forget() method (guarded by a waiter refcount so it never evicts a key someone is actively parked on) is wired into _cleanup_once, which now captures deleted message/approval/teams_outbox/session_relay ids and evicts their notifier keys each sweep; inbox:<session> keys are never evicted (bounded by live identities — later shown by the codex review, session 8, to not actually hold against adversarial identities; see FMC-12). (3) wait_for() now loops on a monotonic deadline, continuing to wait out the remaining timeout after a lost-race wakeup instead of returning early. (4) _cleanup_once gained a delete_grace parameter so a message marked expired this sweep is only pruned in a later sweep, making status=expired observable instead of an opaque NotFoundError; confirmed via existing test_teams_outbox.py/test_session_relay.py tests that teams_outbox/session_relay intentionally keep same-pass mark+delete (their ~30min request timeouts are far shorter than the 7-day store TTL) so left those untouched — resolved the scope-ambiguity the handover flagged without needing to ask, since the existing passing tests were authoritative. Added 7 tests to tests/test_storage.py; each of the 5 regression tests independently confirmed to fail against the pre-fix code via git stash (of store.py only, keeping the new tests) and pass against the fix. Verified: `uv run pytest` (284 passed, up from 277), `uv run ruff check src/ tests/` (clean). All 5 ACs checked. |
| 7 | FMC-6 | Done, 2026-07-21, session 7 | Fixed all 3 tool-pattern deviations from CLAUDE.md's documented contract. (1) pending_approvals (permissions.py) and who (presence.py) now validate their one input param (limit / stale_seconds) and catch ValidationError separately -- confirmed both were live bugs (raw ValueError/TypeError falling into the bare except Exception, returning UNKNOWN_ERROR with no field) via git stash before fixing; get_status (messaging.py) takes zero parameters and cannot raise ValidationError under any input, verified by reading its body, so left unchanged rather than add a dead/unreachable except branch -- same verify-first judgment call FMC-8 established. (2) Added Store.wait_for_pending_approvals(timeout, limit=50) mirroring the existing wait_for_pending_teams_sends pattern; rewired wait_for_pending_approval to call it instead of reaching into store._notifier directly; removed the dead Notifier re-import and the now-unused _approvals_or_none() helper. (3) Rewrote all 9 long-poll tools' timeout Field descriptions (messaging.py x2, permissions.py x2, pubsub.py x1, teams_outbox.py x2, session_relay.py x2) to state their actual enforced cap (600s for await_decision, 300s for the rest) instead of the misleading generic "capped by poll_max_wait_s" wording. Added 6 tests (290 total, up from 284); the 2 regression tests for sub-issue 1 each confirmed to fail against the pre-fix code via git stash. Verified: `uv run pytest` (290 passed), `uv run ruff check src/ tests/` (clean); `ruff format` flags pre-existing drift on store.py/teams_outbox.py confirmed present on dev before this branch (same class FMC-4 already documented), not introduced here. |

## Not queued — needs a human / blocked
(none — all 8 new issues (FMC-9 through FMC-16) classified as agent-resolvable: none require a human physically present, external credentials, or a product decision an agent cannot reasonably make and document. FMC-9 and FMC-13 (channel.py trust model) carry more inherent design ambiguity than the rest — they require inventing a real trust/provenance mechanism rather than a mechanical fix — but this is the same class of judgment call FMC-4 and FMC-8 already made autonomously and documented; not grounds for excluding them from the queue.)

## Session log
- 2026-07-20 — session 0 (init): Inventoried all 7 open FMC issues via `backlog task list`/`backlog task view`; classified all as agent-resolvable (none need a human at hardware or a product decision). Proposed queue order (docs sweep first for lowest risk/context-building, then High→Medium→Low severity, complexity-ordered within Medium); user confirmed the proposed order verbatim. Created this tracker doc, added `.claude/handovers/` to `.gitignore`, created `archive/handovers/`. Noted pre-existing dirty working tree (modified CLAUDE.md/README.md/fmc-1 task file, untracked `.claude/skills/`, `herdr-tmux-shim/`, and the fmc-2..fmc-8 task files themselves) — left untouched, only the tracker doc + .gitignore change were committed by this session. Wrote first handover for cursor FMC-7.
- 2026-07-20 — session 1: Restored from the session-0 handover; ground-truth check found zero drift (HEAD/tracker/FMC-7 status all matched). Resolved FMC-7 on `feature/FMC-7` off `dev` @ `820ab92`: fixed all 6 documentation-drift clusters across CLAUDE.md, README.md, start-session.sh, and a new spawner/README.md. Cursor advances to FMC-3.
- 2026-07-20 — session 2: Restored from the session-1 handover; ground-truth check found zero drift (HEAD/tracker/FMC-3 status all matched, no leftover branches/PRs). Resolved FMC-3 on `feature/FMC-3` off `dev` @ `0c5cfeb`: fixed the process-global self-perpetuating lockout and the non-ASCII bearer-token crash in auth.py, added 3 tests. Cursor advances to FMC-2.
- 2026-07-20 — session 3: Restored from the session-2 handover; ground-truth check found zero drift (HEAD/tracker/FMC-2 status all matched, no leftover branches/PRs, working tree clean and in sync with origin/dev @ dae8924). Resolved FMC-2 on `feature/FMC-2` off `dev` @ `dae8924`: redacted announce_token (and other credential-shaped metadata keys) from who()'s output at the tool boundary in presence.py, reusing logging_config.py's existing sensitive-field blacklist; confirmed the forget/announce owner-token guard is unaffected since it reads the token via its own raw store query; added 2 regression tests. Cursor advances to FMC-8.
- 2026-07-20 — session 4: Restored from the session-3 handover; ground-truth check found zero drift on cursor/status (only a minor drift: the handover's claimed "1 commit ahead of origin/dev, unpushed" had already been pushed by the time this session started — no action needed). Resolved FMC-8 on `feature/FMC-8` off `dev`: confirmed the .mcp.json.example channel-key mismatch is real and exploitable (via research subagent + direct claude-CLI-binary inspection), fixed 4 doc/config files to match channel.py's SERVER_NAME, added 3 regression tests. Cursor advances to FMC-4.
- 2026-07-21 — session 5: Restored from the session-4 handover; ground-truth check found zero drift (dev @ 4161867 matched origin, working tree clean, FMC-4 still To Do, no leftover branches/PRs). Resolved FMC-4 on `feature/FMC-4` off `dev` @ `4161867`: reordered validate_workspace_path's containment/must_exist checks to close the sandbox existence oracle, added a generic size-cap validator wired into all 6 flagged structured fields, added 22 new tests (277 total, up from 257). Cursor advances to FMC-5.
- 2026-07-21 — session 6: Restored from the session-5 handover; ground-truth check found zero drift (dev @ ac5d671 matched origin, working tree clean, FMC-5 still To Do, no leftover branches/PRs). Resolved FMC-5 on `feature/FMC-5` off `dev` @ `ac5d671`: rewrote Notifier (notify_prefix/forget/deadline-looping wait_for) fixing sub-bugs 1-3 together since they share the class, wired forget() into _cleanup_once for sub-bug 2's eviction, added a delete_grace parameter for sub-bug 4's mark-then-delete fix (messages table only — confirmed via existing tests that teams_outbox/session_relay must keep same-pass deletion). Added 7 tests (284 total, up from 277), each of the 5 regression tests confirmed to fail pre-fix via git stash. Cursor advances to FMC-6 (last item in the queue).
- 2026-07-21 — session 7: Restored from the session-6 handover; ground-truth check found zero drift (dev @ 141b7ed matched origin, working tree clean, FMC-6 still To Do, no leftover branches/PRs). Also closed out a leftover agent-team teammate pane (session-0338564a/a50a56e715ded7198) idling from the ad-hoc dogfooding review that discovered FMC-6, per explicit user request at session start. Resolved FMC-6 on `feature/FMC-6` off `dev` @ `141b7ed`: fixed pending_approvals/who's missing ValidationError branches (both confirmed live bugs via git stash), added Store.wait_for_pending_approvals mirroring wait_for_pending_teams_sends, rewrote all 9 long-poll tools' timeout Field descriptions to state their real cap. Added 6 tests (290 total, up from 284). Queue is now empty — this was the last item; campaign complete pending user direction on `init`-ing a fresh queue.
- 2026-07-21 — session 8 (re-init): Previous queue was empty (7/7 resolved, confirmed above). In a separate, prior conversation (context since cleared), ran a second-opinion full-codebase review via OpenAI Codex (gpt-5.6-sol, ultra reasoning effort) against src/fast_mcp_claude/ and tests/; every one of its ~38 findings was independently re-verified against the actual current code by dedicated verification subagents (file/line quoted, behavior traced or reproduced) before being trusted — adjudicated report saved as Backlog doc-2 ("Codex full-codebase review (2026-07-21)"). Of the 15 CRITICAL/HIGH findings, created 8 new bug tasks (FMC-9 through FMC-16), bundling related sub-bugs discovered in the same file per this project's established FMC-3/FMC-5 convention; cross-checked each against the 6 already-Done tasks first to confirm none duplicate prior fixes (two related-but-distinct notes surfaced: one MEDIUM auth.py finding is actually FMC-3's intentional accepted tradeoff, not a bug, and one MEDIUM store.py finding looks like a subtle regression in FMC-5's own retry-loop rewrite — both out of scope for the CRITICAL/HIGH-only cut and not queued here). This session (8, re-init): reused this existing tracker doc rather than creating a duplicate (its own "Queue empty" note explicitly pointed here); inventoried all 8 new issues as agent-resolvable (FMC-9/FMC-13 flagged as more design-ambiguous but not blocked); proposed an isolation/complexity-first order (small isolated fixes, then same-file pairs grouped together, hardest/most ambiguous last); user confirmed this exact order via explicit multiple-choice selection. Committed the tracker update + doc-2 + the 8 new fmc-9..16 task files on `dev`. Noted but did NOT commit an unrelated pre-existing untracked directory, `.claude/skills/codex-review/` (a skill added outside this campaign's scope in an earlier session) — this WILL show up as a dirty-tree hit in the next `/backlog-handover restore`'s preflight check; the user should either commit or otherwise resolve it before running restore next, or explicitly direct the next session to fold it into that session's housekeeping commit. Cursor set to FMC-10. Wrote the session-8 handover.
