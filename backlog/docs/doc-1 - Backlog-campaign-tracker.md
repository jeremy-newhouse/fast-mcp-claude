---
id: doc-1
title: Backlog campaign tracker
type: other
created_date: '2026-07-20 20:49'
updated_date: '2026-07-21 22:22'
---
# Backlog campaign tracker

One issue per session. Protocol: restore → take the cursor issue → feature-branch
lifecycle → advance cursor → append session log → write handover.

## Cursor
**Queue is EMPTY.** All 15 issues from this campaign (FMC-7/3/2/8/4/5/6 from the first
pass, FMC-10/14/11/12/9/13/15/16 from the Codex-review-generated second pass) are
Resolved. Campaign complete pending user direction — run `/backlog-handover init` to
inventory a fresh queue whenever the user wants to continue burning down new backlog
issues.

## Queue (confirmed order)
| # | Issue | Type | One-line note |
| --- | --- | --- | --- |
| (none — queue is empty) | | | |

## Resolved
| # | Issue | Status/date/session | Evidence summary |
| --- | --- | --- | --- |
| 1 | FMC-7 | Done, 2026-07-20, session 1 | Doc-only: fixed the permission-relay implemented-vs-not-yet contradiction (CLAUDE.md + README.md, 3 locations); added launcher.py/session.py/session_hook.py to CLAUDE.md module layout + presence.forget + session_relay 'check' op; corrected traversal-defense claim; softened CLAUDE.md's absolute no-central-hub claim; added README Standalone tooling section (worker-supervisor/spawner/sandbox-runner/start-session.sh) + fixed channel source-attribute example + completed the 8-tool teams_outbox/session_relay table rows; created spawner/README.md; rewrote start-session.sh header comment. Verified via grep/read of current source against every claim; `ruff check src/ tests/` and `bash -n start-session.sh` both passed. All 5 ACs checked. |
| 2 | FMC-3 | Done, 2026-07-20, session 2 | Fixed 2 bugs in src/fast_mcp_claude/auth.py::ApiKeyVerifier.verify_token: (1) process-global self-perpetuating lockout — reordered so a correct token is compared and accepted BEFORE consulting the rate limiter, so it always succeeds even during an active lockout (verified verify_token receives no per-connection identity to scope the limiter to just the attacker); (2) non-ASCII bearer crash — hmac.compare_digest now runs on UTF-8-encoded bytes instead of raw str, so a non-ASCII token yields a clean None/401 instead of an uncaught TypeError, and is correctly counted as a rate-limiter failure. Added 3 tests to tests/test_auth.py covering both scenarios. Verified: `uv run pytest tests/test_auth.py -v` (10 passed), full `uv run pytest` (251 passed), `uv run ruff check src/ tests/` (clean). All 3 ACs checked. |
| 3 | FMC-2 | Done, 2026-07-20, session 3 | Fixed the announce_token leak in who() (src/fast_mcp_claude/tools/presence.py): redact credential-shaped metadata keys (reusing logging_config.py's existing SENSITIVE_LOG_FIELDS_EXACT/SENSITIVE_LOG_SUFFIXES blacklist) from each peer's metadata at the who() tool boundary, not in the shared store.list_presence()/_row_to_presence() layer (existing tests rely on that layer still exposing the raw token to verify the ECA-71 guard end-to-end). forget()/announce()'s owner-token guard needed no change — it reads the token via its own raw SQL query, never through the redacted path. Added 2 tests to tests/test_presence.py. Verified: `uv run pytest tests/test_presence.py -v` (15 passed, all pre-existing tests unmodified and passing), full `uv run pytest` (253 passed), `uv run ruff check src/ tests/` (clean). All 3 ACs checked. |
| 4 | FMC-8 | Done, 2026-07-20, session 4 | Verify-first bug, resolved as CONFIRMED BROKEN (AC#1), not the no-op AC#3 branch. Verified via two independent sources (official Claude Code docs fetched by a research subagent, and direct `strings` inspection of the installed claude CLI v2.1.216 binary) that Claude Code prefixes MCP tool names using the .mcp.json config KEY (`mcp__<key>__<tool>`), never the server's self-declared initialize-handshake name — confirming channel.py's own in-code comment. This meant .mcp.json.example's channel entry key ("claude-channel") really did mismatch channel.py's hardcoded SERVER_NAME ("fast-mcp-claude-channel"), breaking the permission relay's always-allow check for the channel's own reply/send_teams/list_sessions/send_to_session/check_session_message tools for anyone following .mcp.json.example + README.md literally (start-session.sh's generated config already used the correct key, so that path was unaffected). Fixed (AC#2) by aligning every documented/example config key with SERVER_NAME rather than making SERVER_NAME dynamic (narrower, lower-risk): .mcp.json.example, README.md, .claude/commands/worker.md, .claude/settings.example.json. Added 3 regression tests in tests/test_channel.py that parse those files at test time and assert the key/flag equals channel_mod.SERVER_NAME. Verified: `uv run pytest tests/test_channel.py -v` (92 passed), full `uv run pytest` (257 passed), `uv run ruff check src/ tests/` (clean). ACs #1 and #2 checked; #3 left unchecked as not applicable (mutually exclusive with the confirmed-broken outcome). |
| 5 | FMC-4 | Done, 2026-07-21, session 5 | Fixed 2 bugs. (1) Sandbox existence oracle: reordered validate_workspace_path (utils/validation.py) so the WORKSPACE_ROOTS containment check runs before must_exist — empirically confirmed the pre-fix code raised different exception types (PermissionDeniedError vs ValidationError) for an existing vs missing out-of-sandbox path by exec'ing the old function standalone against a tmp dir; post-fix both raise PermissionDeniedError uniformly. Left config.py:232's symlink-resolve logic untouched per the task's own note. (2) Body-size caps: added a generic validate_json_object_size() helper plus MAX_METADATA_BYTES=256_000/MAX_TOOL_INPUT_BYTES=1_000_000/MAX_TOOL_NAME_BYTES=256, wired into all 6 flagged fields (send_prompt.metadata, request_approval.tool_name/tool_input, announce.metadata, request_session_op.payload, complete_session_op.result, request_teams_send.metadata); validate_pubsub_payload refactored to delegate to the same helper. Added 14 validator unit tests + 8 tool-layer wiring tests (2 new files: test_messaging.py, test_permissions.py). Verified: `uv run pytest` (277 passed, up from 257), `uv run ruff check src/ tests/` (clean; pre-existing `ruff format` drift on 9 files, unrelated to this branch, confirmed via git stash). All 3 ACs checked. |
| 6 | FMC-5 | Done, 2026-07-21, session 6 | Fixed all 4 Notifier/long-poll correctness bugs in services/store.py. (1) Broadcast messages now wake every identity-scoped waiter via a new Notifier.notify_prefix(), not just the wildcard inbox:* key. (2) Notifier._events no longer grows unbounded: a new forget() method (guarded by a waiter refcount so it never evicts a key someone is actively parked on) is wired into _cleanup_once, which now captures deleted message/approval/teams_outbox/session_relay ids and evicts their notifier keys each sweep; inbox:<session> keys are never evicted (bounded by live identities — later shown by the codex review, session 8, to not actually hold against adversarial identities; see FMC-12). (3) wait_for() now loops on a monotonic deadline, continuing to wait out the remaining timeout after a lost-race wakeup instead of returning early. (4) _cleanup_once gained a delete_grace parameter so a message marked expired this sweep is only pruned in a later sweep, making status=expired observable instead of an opaque NotFoundError; confirmed via existing test_teams_outbox.py/test_session_relay.py tests that teams_outbox/session_relay intentionally keep same-pass mark+delete (their ~30min request timeouts are far shorter than the 7-day store TTL) so left those untouched — resolved the scope-ambiguity the handover flagged without needing to ask, since the existing passing tests were authoritative. Added 7 tests to tests/test_storage.py; each of the 5 regression tests independently confirmed to fail against the pre-fix code via git stash (of store.py only, keeping the new tests) and pass against the fix. Verified: `uv run pytest` (284 passed, up from 277), `uv run ruff check src/ tests/` (clean). All 5 ACs checked. |
| 7 | FMC-6 | Done, 2026-07-21, session 7 | Fixed all 3 tool-pattern deviations from CLAUDE.md's documented contract. (1) pending_approvals (permissions.py) and who (presence.py) now validate their one input param (limit / stale_seconds) and catch ValidationError separately -- confirmed both were live bugs (raw ValueError/TypeError falling into the bare except Exception, returning UNKNOWN_ERROR with no field) via git stash before fixing; get_status (messaging.py) takes zero parameters and cannot raise ValidationError under any input, verified by reading its body, so left unchanged rather than add a dead/unreachable except branch -- same verify-first judgment call FMC-8 established. (2) Added Store.wait_for_pending_approvals(timeout, limit=50) mirroring the existing wait_for_pending_teams_sends pattern; rewired wait_for_pending_approval to call it instead of reaching into store._notifier directly; removed the dead Notifier re-import and the now-unused _approvals_or_none() helper. (3) Rewrote all 9 long-poll tools' timeout Field descriptions (messaging.py x2, permissions.py x2, pubsub.py x1, teams_outbox.py x2, session_relay.py x2) to state their actual enforced cap (600s for await_decision, 300s for the rest) instead of the misleading generic "capped by poll_max_wait_s" wording. Added 6 tests (290 total, up from 284); the 2 regression tests for sub-issue 1 each confirmed to fail against the pre-fix code via git stash. Verified: `uv run pytest` (290 passed), `uv run ruff check src/ tests/` (clean); `ruff format` flags pre-existing drift on store.py/teams_outbox.py confirmed present on dev before this branch (same class FMC-4 already documented), not introduced here. |
| 8 | FMC-10 | Done, 2026-07-21, session 9 | Fixed both bugs in the auth bootstrap path. (1) server.py fails closed now: extracted build_auth_provider(settings), which raises RuntimeError when MCP_AUTH_ENABLED=true but MCP_API_KEY is None/empty instead of silently constructing FastMCP with auth=None. (2) __main__.py's startup log auth_enabled field now reads a new Settings.mcp_auth_effective property (bool(mcp_api_key) and mcp_auth_enabled) instead of the buggy `mcp_api_key is not None and mcp_auth_enabled`, which misreported true for an empty-string key; the property is a single source of truth so the two can't drift apart again. Added tests/test_server.py (8 tests: 4 for build_auth_provider's raise/success/disabled cases, 4 for mcp_auth_effective's truth table). Verified via git stash that all 8 fail against pre-fix code (ImportError, the helpers didn't exist yet) and pass after the fix; full suite 298 passed (up from 290); `ruff check`/`format --check` clean. All 3 ACs checked. |
| 9 | FMC-14 | Done, 2026-07-21, session 10 | Fixed hook.py's authenticated relay: Client(url, headers={...}) raised a TypeError on every call (fastmcp 3.4.4's Client has no headers param), permanently degrading every authenticated deployment to the local "ask" fallback. Confirmed the actual installed Client API directly (inspect.signature + transport source, not memory/docs): auth accepts httpx.Auth | Literal['oauth'] | str | None, and a plain str is auto-wrapped in BearerAuth. Replaced with Client(url, auth=api_key or None). Added tests/test_hook.py (3 tests): a relay_server fixture serves the REAL request_approval/await_decision/pending_approvals/approve_tool tool functions (rebound to an isolated Store via monkeypatch, not the process singleton) over a real ephemeral HTTP port guarded by the real ApiKeyVerifier; full allow round trip, full deny round trip, wrong-api-key rejection. Confirmed via git stash that both round-trip tests fail against the pre-fix code with the exact predicted TypeError, and pass after the fix. Full suite: 301 passed (up from 298); ruff check clean; ruff format flags the same 9 pre-existing-drift files already documented in FMC-4/6/8, none touched here. All 3 ACs checked. |
| 10 | FMC-11 | Done, 2026-07-21, session 11 | Fixed the file-bridge TOCTOU: validate_workspace_path only proved containment at check time and returned a plain Path, so list_files/read_file/write_file's later os.scandir/read/write calls re-traversed that path string and could be redirected by a symlink swapped in during the gap. Added utils/secure_fs.py (secure_scandir/secure_open_read/secure_open_write), which walk from an already-open workspace-root directory fd and open every remaining path component -- including newly-created parent directories in write_file -- with O_NOFOLLOW via dir_fd, so a swapped symlink at any level raises OSError instead of being followed; write_file's overwrite=False also became atomic (O_EXCL) instead of a separate racy exists() check. Verified os.scandir does NOT support dir_fd on this macOS install (but os.open/os.mkdir do, and scandir accepts a raw fd) via a standalone empirical probe before writing any fix code, same discipline FMC-14 required. Added tests/test_files.py (14 tests): 6 primitive-level TOCTOU simulations (target dir, ancestor dir, target file for read/write, the parent-mkdir race explicitly called out in the task, O_EXCL atomicity), 4 tool-level TOCTOU simulations (monkeypatch validate_workspace_path to inject the swap between check and use) asserting a specific error.code == PERMISSION_DENIED, and 4 happy-path regressions. Confirmed via git stash (tools/files.py only) that all 4 tool-level tests fail against the pre-fix code with success=True (attack succeeds), then restored the fix. Verified: `uv run pytest` (315 passed, up from 301), `uv run ruff check src/ tests/` (clean; ruff format applied to the 2 touched files, the 9 pre-existing-drift files from FMC-4/6/8/14 untouched). All 4 ACs checked. Also updated CLAUDE.md's module layout + File-bridge sandbox bullet to document utils/secure_fs.py. |
| 11 | FMC-12 | Done, 2026-07-21, session 12 | Fixed both store.py bugs. (1) list_pending_teams_sends/list_pending_session_ops now atomically claim (pending -> claimed, under _db_lock) instead of a bare SELECT, mirroring pop_next_for_worker's select+update pattern -- two concurrent hub drain calls can no longer both observe the same row; complete_teams_send/complete_session_op widened to accept status IN (pending, claimed); _cleanup_once's stale-expiry sweep now also catches CLAIMED rows so a crashed-mid-drain row doesn't dangle forever. (2) Notifier.wait_for skips registering an Event at all when timeout<=0, closing the cheap/unlimited-rate fabricated-identity DoS vector; Notifier._events is now an LRU-capped OrderedDict (default 10_000) evicting least-recently-used, non-actively-waited keys once over capacity. An adversarial subagent review (this campaign's established practice for concurrency/security-boundary fixes, per FMC-11/FMC-14) caught a real gap: the waiter refcount protecting a key from capacity eviction was bumped after wait_for's first check() rather than before it, so a flood of concurrent _get() calls during that await could evict a just-registered key before it counted as actively waited -- fixed by moving the bump earlier, with a dedicated regression test confirmed via manual revert-and-rerun to fail against the pre-fix ordering. Added 12 tests (327 total, up from 315); every new/changed assertion confirmed via git stash (or manual revert) to fail pre-fix (including a standalone script that reproduced the literal double-claim). |
| 12 | FMC-9 | Done, 2026-07-21, session 13 | Fixed both critical channel.py trust-boundary bugs. Bug 1 (forgeable triggering_admin): added an optional, distinct Settings.mcp_admin_api_key; auth.py's ApiKeyVerifier now stamps AccessToken.claims={"admin": True} only on a match against that key (unset by default -- nobody is ever admin-trusted); tools/messaging.py::send_prompt clamps a caller-supplied metadata.triggering_admin to (caller_value AND is_admin) via fastmcp.server.dependencies.get_access_token() -- never forces it True, only lets it stay True when the request actually authenticated with the admin key. channel.py's _handle_permission is unchanged -- it already trusted the stored metadata verbatim, now safe since the value can no longer be forged upstream. Bug 2 (operator_direct mislabeling): added a remote-context grace window in channel.py (_Runtime.remote_turn_started_ts + _mark_remote_context/_clear_remote_context/_remote_context_active), marked when an FYI is pushed or when _await_consumption returns _UNKNOWN (ambiguous -- the original turn may still be executing), cleared on _CONSUMED/_DEAD; _handle_send_teams only stamps operator_direct=true when inflight is None AND the grace window is not active, otherwise stamps neither trust flag (the hub's existing logic already fails safe). An adversarial subagent review caught one CRITICAL gap in the first version (a blind 60s timer bypassable by any slower-than-60s turn) -- fixed by extending the window with the same hook-status-file liveness signal Layer C already trusts, plus 3 minor findings. Added 17 tests total (344 final, up from 327), every one confirmed via git stash to fail pre-fix and pass post-fix. Updated .env.example/README.md/CLAUDE.md to document the new admin-key trust model. |
| 13 | FMC-13 | Done, 2026-07-21, session 14 | Fixed both bugs in channel.py's arm/claim/reply state machine (AC#1-4), same file as FMC-9 but a distinct concern. AC#1: presence's channel:true advertisement was previously gated on nothing but cfg.enabled -- wired to rt.initialized (the same notifications/initialized signal the inbox loop already used); the inbox loop's initialized-wait no longer falls through to arm after a 30s timeout, it loops indefinitely staying disarmed. AC#2: _handle_reply only signals reply_event when the mesh relay actually succeeded. AC#3: added _Runtime.bounced_message_id so a late reply after a non-consumption bounce gets a distinguishable warning instead of the generic unknown-id one. AC#4: moved the _push call inside the existing try/finally, closing the leak where a push failure left claimed-message state (and its permission-relay trust) stale indefinitely. An adversarial subagent review found one real, non-blocking gap (bounced_message_id was a scalar, only the MOST RECENT bounce got AC#3's warning) -- fixed by switching to a bounded deque. Added 7 tests total (362 final, up from 354); 6 confirmed via git stash to fail pre-fix/pass post-fix. |
| 14 | FMC-15 | Done, 2026-07-21, session 15 | Fixed all 3 launcher.py bugs around duplicate-instance ownership and lost replies. AC#1/#2: added an owner_confirmed asyncio.Event set on a successful announce and cleared on any refusal (mirrors channel.py's ECA-71 announce_confirmed pattern), gating both the stale-claim reaper and every poll-loop iteration via a new _wait_for_owner_confirmed_or_reconnect helper — a rogue second launcher instance whose announce is refused (IDENTITY_LIVE_ELSEWHERE) now never reaps or claims. AC#3: task replies now route through a new _ClientBox holding the bridge's CURRENT connection (updated on every reconnect). AC#4: _shutdown opens its own fresh connection for the launcher_shutdown reply sweep instead of reusing the bridge's connection, which cancellation has already torn down by the time _shutdown runs. An adversarial subagent review found and fixed 3 further issues in a follow-up commit: a persistently bad bearer could have deadlocked the new gate forever (fixed with an owner_wait_abandoned escape valve); the shutdown sweep's fresh connection had no timeout and could hang indefinitely (bounded with asyncio.wait_for); and the gate's recheck only happened between polls, not during one, letting an already-claimed message slip through a mid-poll refusal (fixed by bouncing it). Added 5 regression tests total across both commits (367 final, up from 362). |
| 15 | FMC-16 | Done, 2026-07-21, session 16 | Fixed the last 3 launcher.py bugs (AC#1-3), same file as FMC-15. AC#1: _run_claude now streams stdout/stderr through a new _read_capped() (deque-based rolling TAIL window, O(cap) memory) capped at MAX_SUBPROCESS_OUTPUT_BYTES=4MiB per stream, replacing proc.communicate()'s full-buffer read; drained post-exit via a bounded _drain_capped(). AC#2: _kill_group now polls group-wide liveness via a new _group_alive() (os.killpg(pgid,0) existence probe) for up to KILL_GRACE_S instead of only awaiting the group leader, force-SIGKILLing the group if a grandchild is still alive. AC#3: _approval_relay_server gained a 'ready' Event set once its unix socket is confirmed bound+listening; a new _run_approval_relay_supervised() runs it under a restart-with-backoff supervisor tracking a 'relay_healthy' Event; _bridge gained an optional relay_healthy param gating gated-task claiming both pre-poll and in a post-poll residual recheck, with a dedicated _wait_for_relay_healthy_or_reconnect() helper (deliberately no abandon-and-proceed escape valve, unlike the owner-token gate, since AC#3's whole point is failing closed forever on a persistently-unreachable relay). An adversarial subagent review (the 7th consecutive hit in this campaign for this class of fix) found and fixed 1 blocking bug (_group_alive only caught ProcessLookupError from os.killpg(pgid,0), but the reviewer directly reproduced a PermissionError under a pgid-recycling race that aborted _kill_group before its SIGKILL fallback ever ran) and 1 flaky-test timing issue (the new AC#2 regression test's budget was too tight for a fresh python3 interpreter to reach os.fork() before the timeout fired; fixed to match the sibling test's proven-reliable budget, reverified 15/15 stable in isolation). Added 7 regression tests (374 final, up from 367); every one confirmed via git stash to fail against pre-fix launcher.py and pass post-fix. Verified: `uv run pytest` (374 passed), `uv run ruff check src/ tests/` (clean; pre-existing ruff-format drift confirmed unchanged via git stash, same class as prior sessions). This was the LAST item in the queue — campaign complete (15/15 resolved). |

## Not queued — needs a human / blocked
(none — all 15 issues from this campaign were agent-resolvable.)

## Session log
- 2026-07-20 — session 0 (init): Inventoried all 7 open FMC issues via `backlog task list`/`backlog task view`; classified all as agent-resolvable (none need a human at hardware or a product decision). Proposed queue order (docs sweep first for lowest risk/context-building, then High→Medium→Low severity, complexity-ordered within Medium); user confirmed the proposed order verbatim. Created this tracker doc, added `.claude/handovers/` to `.gitignore`, created `archive/handovers/`. Noted pre-existing dirty working tree (modified CLAUDE.md/README.md/fmc-1 task file, untracked `.claude/skills/`, `herdr-tmux-shim/`, and the fmc-2..fmc-8 task files themselves) — left untouched, only the tracker doc + .gitignore change were committed by this session. Wrote first handover for cursor FMC-7.
- 2026-07-20 — session 1: Restored from the session-0 handover; ground-truth check found zero drift (HEAD/tracker/FMC-7 status all matched). Resolved FMC-7 on `feature/FMC-7` off `dev` @ `820ab92`: fixed all 6 documentation-drift clusters across CLAUDE.md, README.md, start-session.sh, and a new spawner/README.md. Cursor advances to FMC-3.
- 2026-07-20 — session 2: Restored from the session-1 handover; ground-truth check found zero drift (HEAD/tracker/FMC-3 status all matched, no leftover branches/PRs). Resolved FMC-3 on `feature/FMC-3` off `dev` @ `0c5cfeb`: fixed the process-global self-perpetuating lockout and the non-ASCII bearer-token crash in auth.py, added 3 tests. Cursor advances to FMC-2.
- 2026-07-20 — session 3: Restored from the session-2 handover; ground-truth check found zero drift (HEAD/tracker/FMC-2 status all matched, no leftover branches/PRs, working tree clean and in sync with origin/dev @ dae8924). Resolved FMC-2 on `feature/FMC-2` off `dev` @ `dae8924`: redacted announce_token (and other credential-shaped metadata keys) from who()'s output at the tool boundary in presence.py, reusing logging_config.py's existing sensitive-field blacklist; confirmed the forget/announce owner-token guard is unaffected since it reads the token via its own raw store query; added 2 regression tests. Cursor advances to FMC-8.
- 2026-07-20 — session 4: Restored from the session-3 handover; ground-truth check found zero drift on cursor/status (only a minor drift: the handover's claimed "1 commit ahead of origin/dev, unpushed" had already been pushed by the time this session started — no action needed). Resolved FMC-8 on `feature/FMC-8` off `dev`: confirmed the .mcp.json.example channel-key mismatch is real and exploitable (via research subagent + direct claude-CLI-binary inspection), fixed 4 doc/config files to match channel.py's SERVER_NAME, added 3 regression tests. Cursor advances to FMC-4.
- 2026-07-21 — session 5: Restored from the session-4 handover; ground-truth check found zero drift (dev @ 4161867 matched origin, working tree clean, FMC-4 still To Do, no leftover branches/PRs). Resolved FMC-4 on `feature/FMC-4` off `dev` @ `4161867`: reordered validate_workspace_path's containment/must_exist checks to close the sandbox existence oracle, added a generic size-cap validator wired into all 6 flagged structured fields, added 22 new tests (277 total, up from 257). Cursor advances to FMC-5.
- 2026-07-21 — session 6: Restored from the session-5 handover; ground-truth check found zero drift (dev @ ac5d671 matched origin, working tree clean, FMC-5 still To Do, no leftover branches/PRs). Resolved FMC-5 on `feature/FMC-5` off `dev` @ `ac5d671`: rewrote Notifier (notify_prefix/forget/deadline-looping wait_for) fixing sub-bugs 1-3 together since they share the class, wired forget() into _cleanup_once for sub-bug 2's eviction, added a delete_grace parameter for sub-bug 4's mark-then-delete fix (messages table only — confirmed via existing tests that teams_outbox/session_relay must keep same-pass deletion). Added 7 tests (284 total, up from 277), each of the 5 regression tests confirmed to fail pre-fix via git stash. Cursor advances to FMC-6 (last item in the queue).
- 2026-07-21 — session 7: Restored from the session-6 handover; ground-truth check found zero drift (dev @ 141b7ed matched origin, working tree clean, FMC-6 still To Do, no leftover branches/PRs). Also closed out a leftover agent-team teammate pane (session-0338564a/a50a56e715ded7198) idling from the ad-hoc dogfooding review that discovered FMC-6, per explicit user request at session start. Resolved FMC-6 on `feature/FMC-6` off `dev` @ `141b7ed`: fixed pending_approvals/who's missing ValidationError branches (both confirmed live bugs via git stash), added Store.wait_for_pending_approvals mirroring wait_for_pending_teams_sends, rewrote all 9 long-poll tools' timeout Field descriptions to state their real cap. Added 6 tests (290 total, up from 284). Queue is now empty — this was the last item; campaign complete pending user direction on `init`-ing a fresh queue.
- 2026-07-21 — session 8 (re-init): Previous queue was empty (7/7 resolved, confirmed above). In a separate, prior conversation (context since cleared), ran a second-opinion full-codebase review via OpenAI Codex (gpt-5.6-sol, ultra reasoning effort) against src/fast_mcp_claude/ and tests/; every one of its ~38 findings was independently re-verified against the actual current code by dedicated verification subagents (file/line quoted, behavior traced or reproduced) before being trusted — adjudicated report saved as Backlog doc-2 ("Codex full-codebase review (2026-07-21)"). Of the 15 CRITICAL/HIGH findings, created 8 new bug tasks (FMC-9 through FMC-16), bundling related sub-bugs discovered in the same file per this project's established FMC-3/FMC-5 convention; cross-checked each against the 6 already-Done tasks first to confirm none duplicate prior fixes. This session (8, re-init): reused this existing tracker doc rather than creating a duplicate; inventoried all 8 new issues as agent-resolvable; proposed an isolation/complexity-first order (small isolated fixes, then same-file pairs grouped together, hardest/most ambiguous last); user confirmed this exact order via explicit multiple-choice selection. Cursor set to FMC-10. Wrote the session-8 handover.
- 2026-07-21 — session 9: Restored via /backlog-handover init (user redirected to restore since the campaign was already re-armed from session 8). Ground-truth check found zero drift beyond an already-noted trap (resolved). Resolved FMC-10 on `feature/FMC-10` off `dev` @ `3d906be`: extracted server.py's auth-provider decision into build_auth_provider(settings), which now raises RuntimeError instead of silently starting unauthenticated when MCP_AUTH_ENABLED=true but MCP_API_KEY is empty/unset; added Settings.mcp_auth_effective as the single source of truth. Added tests/test_server.py (8 tests); full suite 298 passed (up from 290). Cursor advances to FMC-14.
- 2026-07-21 — session 10: Restored from the session-9 handover; zero drift. Resolved FMC-14 on `feature/FMC-14` off `dev` @ `fae2ec3`: verified fastmcp 3.4.4's actual installed Client API directly rather than trusting memory; replaced hook.py's unsupported Client(url, headers={...}) with Client(url, auth=api_key or None). Added tests/test_hook.py (3 tests). Full suite 301 passed. Cursor advances to FMC-11.
- 2026-07-21 — session 11: Restored from the session-10 handover; zero drift. Resolved FMC-11 on `feature/FMC-11` off `dev` @ `29b3262`: added utils/secure_fs.py's dir_fd/O_NOFOLLOW-based helpers, closing the file-bridge TOCTOU window. Added tests/test_files.py (14 tests). Full suite 315 passed. Cursor advances to FMC-12.
- 2026-07-21 — session 12: Restored from the session-11 handover; zero drift. Resolved FMC-12 on `feature/FMC-12` off `dev` @ `c171a98`: made list_pending_teams_sends/list_pending_session_ops atomically claim, closing the double-drain race; capped Notifier._events as an LRU-evicting OrderedDict. An adversarial review caught a real refcount-ordering gap, fixed. Added 12 tests (327 total). Cursor advances to FMC-9.
- 2026-07-21 — session 13: Restored from the session-12 handover; zero drift. Resolved FMC-9 on `feature/FMC-9` off `dev` @ `1f4a425`: fixed the forgeable-triggering_admin bug via a distinct MCP_ADMIN_API_KEY, and the operator_direct-mislabeling bug via a remote-context grace window. An adversarial review caught a CRITICAL gap (blind 60s timer bypassable by a slower turn), fixed. Added 17 tests (344 total). Cursor advances to FMC-13.
- 2026-07-21 — session 14: Restored from the session-13 handover; zero drift. Resolved FMC-13 on `feature/FMC-13` off `dev` @ `5999e9e`: fixed channel.py's arm/claim/reply state machine (AC#1-4). Added 7 tests (362 total). Cursor advances to FMC-15.
- 2026-07-21 — session 15: Restored from the session-14 handover; zero drift. Resolved FMC-15 on `feature/FMC-15` off `dev` @ `f5b7eea`: fixed launcher.py's duplicate-instance ownership and lost-reply bugs (AC#1-4). An adversarial review found and fixed 3 further issues in a follow-up commit. Added 5 tests (367 total). Cursor advances to FMC-16 — the LAST item in the queue.
- 2026-07-21 — session 16: Restored from the session-15 handover; ground-truth check found zero drift (dev @ 62bfd32 matched origin, working tree clean, FMC-16 still To Do, no leftover branches/PRs, launcher.py's most recent touches confirmed to be FMC-15's own commits as expected). Resolved FMC-16 on `feature/FMC-16` off `dev` @ `62bfd32`: fixed the last 3 launcher.py bugs (AC#1-3) — bounded/streamed subprocess output (_read_capped, a rolling tail window), group-wide process-kill verification (_group_alive polling), and an approval-relay readiness+health gate (_run_approval_relay_supervised + relay_healthy Event) that the inbox-claiming bridge now blocks on before claiming any gated task. An adversarial subagent review (the 7th consecutive hit in this campaign for this fix class) found and fixed 1 blocking bug (_group_alive missing PermissionError handling under a pgid-recycling race, directly reproduced by the reviewer) and 1 flaky-test timing issue (fixed, reverified 15/15 stable). Added 7 regression tests (374 final, up from 367), every one confirmed via git stash to fail pre-fix/pass post-fix. **Queue is now empty — all 15 FMC issues from this campaign are Resolved. Campaign complete pending user direction; run `/backlog-handover init` for a fresh queue.**
