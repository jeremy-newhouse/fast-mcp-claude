# Handover — resolve FMC-13 (channel.py incomplete arming gate + race-prone claimed-message state) — sixth issue of the 8-issue queue (FMC-9..16)

**Date**: 2026-07-21 | **Grounded against**: `dev` @ `5999e9e` (1 commit ahead of `origin/dev`, unpushed at write time — push it in restore's preflight), clean | **Tracker**: doc-1

## Paste-ready prompt for the next session

```
Run /backlog-handover restore in /Users/jdnewhouse/repos/fast-mcp-claude. Tracker: doc-1.
Cursor: FMC-13 -- channel.py has two bugs in the channel sidecar's arm/claim/reply state
machine (distinct from FMC-9's trust-boundary bugs, just fixed, same file). (1) The
documented two-part arming gate (channel_enabled AND actually launched with
--dangerously-load-development-channels) is only one part in code -- nothing ever checks
the SDK actually loaded dev channels, and a 30s timeout waiting for the "initialized"
readiness signal proceeds to arm anyway instead of staying disarmed. (2) Claimed-message
state (rt.inflight) is race-prone three ways: (2a) the reply handler signals "consumed"
whenever the reply is addressed to the claimed message, even if the mesh relay call
itself failed; (2b) a late genuine reply arriving after a non-consumption bounce gets a
generic "unknown/already-finalized" warning indistinguishable from a typo'd message id;
(2c) the claimed-message slot is set and pushed BEFORE the try/finally that's supposed to
clear it -- if the push itself raises, the slot leaks indefinitely and a later, unrelated
permission request (including the operator's own local work) gets evaluated against the
stale message's triggering_admin metadata instead of falling through to the local
terminal dialog. Queue order (isolation/complexity-first: FMC-10 [done], FMC-14 [done],
FMC-11 [done], FMC-12 [done], FMC-9 [done], FMC-13, FMC-15, FMC-16) confirmed by the user
on 2026-07-21 -- do not re-ask before taking the next item.
```

## State
| Item | Status |
| --- | --- |
| Branch | `dev` @ `5999e9e`, clean |
| Sync with origin | 1 ahead / 0 behind `origin/dev` (this session's archive-handover commit not yet pushed — push it as part of restore's preflight, or it happens automatically if you push before starting FMC-13's branch) |
| Leftover `feature/*` branches (this campaign) | none real. `git branch -a` still lists `remotes/origin/feature/FMC-9` — confirmed via `git ls-remote --heads origin feature/FMC-9` (empty output) that the remote branch IS deleted; this is a stale local remote-tracking-ref cache, clears on `git fetch --prune`. Not actual litter. |
| Unrelated branches present | `chore/start-session-symlinkable`, `feat/eca-100-lane-reconfig`, `feat/eca-72-supervisor-pre-adoption`, `feat/session-to-session-messaging`, `feat/teams-relay`, `fix/teams-outbox-operator-direct` — all pre-existing, unrelated to this campaign's FMC- numbering; not campaign litter, do not touch. |
| Open PRs | none (`gh pr list --state open` empty) |
| Tracker cursor | FMC-13 (doc-1, updated and committed this session) |
| FMC-13 task status | To Do, unassigned, Priority High, Type bug |
| Queue after FMC-13 | FMC-15, FMC-16 (2 more after this one) |

## Next steps
1. Preflight per the skill: confirm `git status --porcelain` is clean and push the 1-ahead commit to `origin/dev` if not already done.
2. `git checkout -b feature/FMC-13 dev`.
3. Read `backlog instructions task-execution`; `backlog task view FMC-13 --plain` for the full self-contained description (already reviewed this session — see Critical context below for a summary); mark In Progress + assign; record implementation plan.
4. **The task description's cited line numbers are STALE — do not trust them.** They were written before two rounds of unrelated changes landed on `channel.py`: (a) this session's own FMC-9 fix (added `_Runtime.remote_turn_started_ts`, `_mark_remote_context`/`_clear_remote_context`/`_remote_context_active`, restructured the `_await_consumption` call site's try/except), and (b) a separate, out-of-campaign session's `ECA-83` work (commits `e3c30f0`/`235ceb3` on `dev`, landed concurrently while this session was working FMC-9 and required a rebase to merge past) that added `rt.last_activity_ts`/`degraded_activity_ts`/`hookless_warning_logged`, a `baseline_activity_ts` parameter to `_await_consumption`, and permission-request-activity-based re-arming in `_maybe_rearm`. Read the CURRENT `src/fast_mcp_claude/channel.py` in full before touching anything.
5. Locate the four things FMC-13 actually describes in the current code: (a) `_resolve_config`'s `enabled` computation (search for `args.enabled if args.enabled is not None else`) and the inbox loop's `rt.initialized.wait()` with its 30s timeout (search for `session did not signal initialized`) — AC#1's arming-gate bug; (b) `_handle_reply` (search `async def _handle_reply`) unconditionally setting `_RT.reply_event.set()` whenever the reply's `message_id` matches the in-flight one, regardless of whether `_mesh_reply`'s return value was truthy — AC#2; (c) the non-consumption bounce path in `_inbox_loop` (search `_NON_CONSUMPTION_BOUNCE`) clearing `rt.inflight` before/via the bounce, and `_handle_reply`'s fallback warning text (search `WARNING: reply NOT recorded`) — AC#3; (d) the claim sequence's `rt.inflight = msg` / `await _push(...)` ordering relative to the try block wrapping only `_await_consumption` — AC#4 (note: this session's FMC-9 fix already added an `except BaseException` around `_await_consumption` itself for a *different*, narrower reason — defense-in-depth for the remote-context grace window — that does NOT close AC#4's gap, since the push call happens *before* that try block even starts; read the actual current ordering, don't assume the FMC-9 change already fixed this).
6. **AC#1 is the most design-ambiguous item.** There is no obvious existing signal in `channel.py` for "Claude Code actually loaded development channels" beyond the `notifications/initialized` handshake the tee already watches for — investigate whether MCP's initialize response or capabilities negotiation carries anything channel-specific to check, or whether the fix is narrower: just flip the timeout's default from "proceed" to "stay disarmed" (which directly satisfies the AC's second clause: "a timeout waiting for that signal results in staying disarmed rather than proceeding to arm anyway") even if a fully positive can't-be-spoofed signal for dev-channels-loaded doesn't exist. Don't over-invent — re-read the AC's exact wording before deciding scope, same judgment call FMC-9 made for its own two bugs.
7. Add regression tests (AC#5): one per named case (arming-gate timeout-stays-disarmed, failed-mesh-reply-does-not-advance, late-reply-after-bounce gets a distinguishable message, push-failure-clears-claimed-state AND doesn't leak into an unrelated permission request), each confirmed to fail pre-fix / pass post-fix via `git stash`. Per FMC-14's lesson (see Do not repeat below), assert the *specific* behavior distinction, not a bare truthy/exception check.
8. Read `backlog instructions task-finalization` before checking any AC. Verify AC#1-5 with objective evidence.
9. Update the tracker (doc-1) on the branch: move FMC-13 to Resolved, advance cursor to FMC-15, append the session-14 log entry.
10. Commit, review (adversarial subagent strongly recommended — this is a concurrency/state-machine fix in the SAME security-adjacent file as FMC-9, and this campaign's adversarial review has now caught a real bug in every one of the last three security/concurrency fixes: FMC-11, FMC-12, and FMC-9's own second round), push, open+merge PR into `dev` via `gh pr merge --rebase --delete-branch`. **Check `origin/dev` for concurrent unrelated commits BEFORE assuming a clean merge** — this exact thing happened during FMC-9 (an out-of-campaign `ECA-83` session landed 2 commits touching `channel.py` while this session worked, requiring a `git rebase origin/dev` with manual conflict resolution before the PR would merge; see Critical context below for how that was handled). Sync local `dev`, delete local branch, verify remote deletion with `git ls-remote --heads origin feature/FMC-13`.
11. Archive this handover to `archive/handovers/` (check for name collisions — `-2.md` through `-7.md` already exist for 2026-07-21, so this becomes `-8.md`), commit, write the session-14 handover for cursor FMC-15, push `dev`.

## Critical context / traps
- **This campaign's tracker is doc-1, not doc-2.** doc-2 is the Codex full-codebase review report FMC-9..16 were generated from — read it only if FMC-13's own self-contained description isn't enough.
- **`channel.py` is being actively touched by work OUTSIDE this campaign.** During FMC-9's session, a separate concurrent session landed `fix(channel): harden liveness fast-signal against queued-turn false-bounce` and `fix(channel): require baseline_activity_ts, widen residual disclosure` (refs `ECA-83`, not this campaign's FMC- numbering) directly on `dev` while FMC-9 was in progress — the PR merge failed with "not mergeable", requiring `git fetch origin dev && git rebase origin/dev`, manual resolution of two real conflicts (an additive `_Runtime` dataclass-field conflict, trivially resolved by keeping both; and a conflict in the `_await_consumption` call site + the surrounding try/except where FMC-9's Bug 2 fix and ECA-83's new `baseline_activity_ts` parameter both touched the same lines, resolved by keeping ECA-83's 4-arg call signature wrapped in FMC-9's `except BaseException` structure), then `git push --force-with-lease` before the PR would merge. **Expect this again for FMC-13** — re-check `git log origin/dev` for anything unexpected before opening the PR, and don't assume the task description's file/line references still match reality (see Next steps item 4).
- **This is explicitly a bugfix task, not a request to redesign the state machine from scratch.** The task's acceptance criteria are narrow and behavioral (AC#1-5 above) — satisfy those without inventing unnecessary new abstractions unless the narrow fixes genuinely require it (same judgment call FMC-4/FMC-8/FMC-9/etc. already made autonomously and documented).
- **FMC-9 (just resolved this session) is in the same file** but a fully separate, already-merged concern (forgeable `triggering_admin`/`operator_direct` trust, not the arm/claim/reply race conditions FMC-13 targets) — no code overlap expected beyond both living in `channel.py`, just thematic/file proximity. Don't conflate the two; FMC-13's AC#4 (stale claimed-state leaking into an unrelated permission request) is a genuinely different bug from FMC-9's Bug 1 (forged `triggering_admin` metadata), even though both ultimately affect what the permission relay auto-allows.
- **From FMC-9's second round (this session)**: an adversarial subagent review of the FIRST version of FMC-9's Bug 2 fix caught a real CRITICAL gap — a blind fixed-duration timer is trivially bypassable by any operation that legitimately takes longer than the timer to complete (the review demonstrated this isn't a theoretical edge case for agentic work). If FMC-13's fix for AC#1 or AC#3 involves any timeout/grace-period heuristic, sanity-check it against "does this actually scale with realistic operation duration, or is it a blind clock that a sufficiently slow legitimate case bypasses?" before considering it done.
- **From FMC-14's session (session 10)**: an adversarial subagent review caught a real bug in a new test file — a bare `pytest.raises(Exception)` regression test that would also pass against the very bug it was meant to catch. Always assert the *specific* exception type/attribute/state transition a fix's regression test is meant to distinguish, not a bare truthy/exception check.
- **From FMC-11/FMC-12/FMC-9's sessions**: adversarial subagent review of a security-boundary/concurrency fix has now caught a real, previously-unnoticed bug in every one of the last three such fixes in this campaign (a discarded `os.write()` return value in FMC-11; a waiter-refcount-vs-eviction ordering gap in FMC-12; a blind-timer bypass in FMC-9). This is no longer just "worth considering" — budget for it as a required step on any concurrency/security-boundary fix, which FMC-13 clearly is.
- **`gh pr merge --delete-branch` works cleanly when the tree is clean AND `origin/dev` hasn't moved under the PR.** When it HAS moved (as happened this session), the fix is `git fetch && git rebase origin/dev` (resolve conflicts commit-by-commit through the rebase, don't try to squash/skip), re-run the full test suite + ruff after the rebase completes (don't trust that individual commits still pass in isolation), `git push --force-with-lease`, then retry `gh pr merge --rebase --delete-branch`. The remote-tracking ref for a deleted branch can linger in `git branch -a` until a `git fetch --prune` — that's a stale local cache, not an actual leftover branch; `git ls-remote --heads origin feature/<KEY>` is the authoritative check.

## Do not repeat
- Don't re-run `/backlog-handover init` when a tracker/queue/cursor/handover already exist and are current — this is the established norm for this campaign (confirmed explicitly in session 9).
- Don't assume a library's/framework's API shape from memory or general docs — verify against the actual installed/available version/platform before choosing an approach. FMC-14 (fastmcp Client API), FMC-11 (`os.scandir` dir_fd support), FMC-12 (`_db_lock`'s actual scope), and FMC-9 (`fastmcp.server.dependencies.get_access_token()`'s actual resolution behavior, verified by reading the installed SDK source rather than assuming) all hit this discipline; the same applies here if channel.py's arm/claim/reply fix touches anything in the MCP SDK's initialize/capabilities surface.
- Don't write a regression test with a bare `pytest.raises(Exception)` (or equivalent loose assertion) when the whole point is distinguishing "behaves correctly for the right reason" from "happens to also pass for an unrelated reason." FMC-14's adversarial review caught exactly this.
- Don't trust a single self-review as sufficient for a concurrency/state-machine fix — FMC-11's, FMC-12's, and FMC-9's own adversarial subagent reviews each found a real bug the self-review missed. Budget for that extra pass, and don't declare a heuristic fix "done" without asking whether it's bypassable by a realistic slow-path case (the exact FMC-9 lesson above).
- Don't assume `origin/dev` is still at the SHA you branched from when opening the PR — re-check, since concurrent out-of-campaign work can and did land mid-session this time.
